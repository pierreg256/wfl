// Generated by CoffeeScript 1.3.3
(function() {
  var inspect, models;

  inspect = (require('eyes')).inspector();

  models = require("../lib/models");

  exports.run = function(options) {
    var activitiOpt, activity;
    inspect(options);
    activitiOpt = {
      'accessKeyId': options.accessKeyId,
      'secretAccessKey': options.secretAccessKey,
      'region': options.region,
      'domain': options.domain,
      'taskList': {
        'name': options.taskList
      }
    };
    return activity = new models.Activity(activitiOpt, function(err, task) {
      if (err != null) {
        console.log("Error while executing: new Activity");
      }
      return task.respondCompleted(activitiOpt, function(error, result) {
        inspect(error, "error");
        return inspect(result, "result");
      });
    });
  };

}).call(this);
